/* Untyped file generated from Subscriber.res by genType. */
/* eslint-disable */

import {connect as connectNotChecked} from 'mqtt';

// In case of type error, check the type of 'connect' in 'Subscriber.re' and 'mqtt'.
export const connectTypeChecked = connectNotChecked;

// Export 'connect' early to allow circular import from the '.bs.js' file.
export const connect = connectTypeChecked;
